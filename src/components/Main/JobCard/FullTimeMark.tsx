import React from 'react'
import styled from 'styled-components'

const FullTimeMark = styled.span`
  font-size: 14px;
  padding: 6px 8px;
  border: 1px solid;
  border-radius: 4px;
`

export default () => (
  <FullTimeMark>Full time</FullTimeMark>
)
